# ─────────────────────────────────────────────────────────────────────────────
# Secrets — never commit
# ─────────────────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ─────────────────────────────────────────────────────────────────────────────
# User / customer data — never commit
# ─────────────────────────────────────────────────────────────────────────────
# Local data store (user annotations, logs, groups) + SQLite WAL sidecar files
*.db
*.db-wal
*.db-shm

# Playwright MCP captures: page snapshots (.yml) and console logs taken against
# the LIVE dashboard — these embed real price/part/customer rows. NEVER commit.
.playwright-mcp/

# Stray data exports that may hold real rows
*.xlsx
*.xls
*.csv
*.bak
*.dump
*.parquet

# Internal infrastructure documentation (server/network details) — do not publish
infrastructure Document/
*.pdf

# ─────────────────────────────────────────────────────────────────────────────
# TLS material
# ─────────────────────────────────────────────────────────────────────────────
*.pfx
*.pem
*.key
*.crt

# ─────────────────────────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# ─────────────────────────────────────────────────────────────────────────────
# Node / frontend build artifacts
# ─────────────────────────────────────────────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo
.vite/

# ─────────────────────────────────────────────────────────────────────────────
# Editor / OS / local tooling
# ─────────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.claude/settings.local.json
**/.claude/settings.local.json
